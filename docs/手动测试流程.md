# 手动测试流程

> 本文档是票次元所有手动测试流程的**单一权威源**。每次 ship 新功能必须同步更新"已 ship 的功能测试入口"表格。
> CLAUDE.md 通过"测试流程的元规则"指向本文。

---

## 触发时机矩阵

| 时机 | 跑什么 | 谁跑 |
|------|--------|------|
| **commit 前** | 本地 `cd apps/web && npm run build` + `cd apps/mobile && npx tsc --noEmit` | 开发者 |
| **push 前** | `docker compose build web`（CLAUDE.md 铁律：本地 npm build 不可信） | 开发者 |
| **CI 绿后** | 本文件"已 ship 的功能测试入口" 中**本次改动相关项** | 开发者 / 测试者 |
| **大版本发布前** | 全量过一遍（含 [冒烟测试](冒烟测试清单.md) + [Web/Mobile 一致性](Web-Mobile功能对齐审计.md)） | 测试者 |
| **改 admin 流程后** | 单跑 [admin 功能清单](admin-功能测试清单.md) | 开发者 |
| **升依赖 / 改环境后** | 单跑 [一致性测试清单](一致性测试清单.md) | 开发者 |

---

## 测试分类总览

| 分类 | 文档 | 用途 |
|------|------|------|
| **冒烟测试**（P0 部署后验收） | [冒烟测试清单.md](冒烟测试清单.md) | deploy 后 5 分钟内验通核心流程 |
| **回归测试**（UI/UX 改动） | 本文件 + [测试数据使用说明.md](测试数据使用说明.md) | 改完看视觉/交互不退步 |
| **跨端一致性** | [Web-Mobile功能对齐审计.md](Web-Mobile功能对齐审计.md) | 新功能 ship 时核对两端都有 |
| **Admin 后台** | [admin-功能测试清单.md](admin-功能测试清单.md) | admin 改动后逐项过 |
| **依赖一致性** | [一致性测试清单.md](一致性测试清单.md) | 环境变量 / 版本号 / 配色 |

新增清单时**必须**也加入此表格。

---

## 测试数据准备（必读）

测试账号、活动、帖子、对话由 `apps/web/scripts/seed-test-data.js` 灌入。完整命令、数据清单、清理流程见 [测试数据使用说明.md](测试数据使用说明.md)。

**5 个测试账号**（密码统一 `Test123456!`）：
- `13900000001` 测试明星 A（认证 / 主测试账号）
- `13900000002` 测试粉丝 B（与 A 互关 + 80 条历史私信）
- `13900000003` 测试用户 C（无头像 / 单向关注 A）
- `13900000004` 测试用户 D（A 关注的）
- `13900000099` 测试管理员（admin 后台测试用）

**ECS 上灌数据**：
```bash
ssh root@8.159.155.134
cd ~/piaociyuan && git pull origin main
docker cp apps/web/scripts/seed-test-data.js piaociyuan-web:/app/
docker compose exec web node /app/seed-test-data.js
```

测完清理：
```bash
docker cp apps/web/scripts/cleanup-test-data.js piaociyuan-web:/app/
docker compose exec web node /app/cleanup-test-data.js
```

---

## 已 ship 的功能测试入口（按时间倒序）

> 改动 ID 以 commit 短 hash 或 sprint/batch 命名。最新功能在最上方。

### 2026-05-11 — 微信小程序 Phase 0+1（apps/wechat）

| 改动 | 测试入口 | 期望效果 |
|---|---|---|
| **新 workspace apps/wechat 存在** | `ls apps/wechat` | 看到 src/ config/ project.config.json package.json README.md |
| **Taro 编译** | `cd apps/wechat && npm install && npm run build:weapp` | 产出 `apps/wechat/dist/`，无 error；warn 可接受 |
| **微信开发者工具打开** | 开 DevTools → "导入项目" → 选 `apps/wechat/` 根目录 → 填正确 AppID | 编译成功 → 进入登录页 |
| **微信一键登录** | 点"微信一键登录"按钮（DevTools 自动 mock wx.login） | 后端 `/api/auth/wechat-login` 返 200；前端跳到主页 tab |
| **主页活动列表** | 进主页 tab | 调用 `/api/events` 拉到列表；至少 1 张活动卡片 |
| **全部活动分类筛选** | 切到"活动" tab → 点"演唱会" / "音乐节"等 | 列表按分类过滤；点击卡片 toast 提示"Phase 2 上线" |
| **搜索三 tab** | 切到"搜索" tab → 输入"测" → 回车 | user / post / event 三 tab 都有结果（如 DB 有匹配数据） |
| **个人中心** | 切到"我的" tab | 头像 + 昵称 + 账号 ID 显示；点"退出登录"→ 二次确认 → 回到登录页 |
| **通知列表** | 个人中心 → "🔔 通知" / 或直接 navigateTo | 通知列表显示；未读项有红点 |
| **后端 wechat-login endpoint** | 用 13900000001 (A) 在 web 端登录确认正常 | apps/web 现有登录流程完全不动，新加的 `/api/auth/wechat-login` 仅服务于小程序 |
| **CLAUDE.md 章节** | grep "微信小程序" CLAUDE.md | 看到铁律 4 条 + 指向 apps/wechat/README.md |

### 2026-05-11 — UI Bug 修复 + 全局文字颜色统一 + 新功能交互审计

| 改动 | 测试入口 | 期望效果 |
|---|---|---|
| **RightSidebar 私信不崩** | A 创建群聊后回主页 → 点右侧栏顶部箭头展开 → 点"💬 私信"tab | 不报错；列表**只显示私聊**（群聊不在右侧栏显示，去 /messages 看） |
| **Logo 不变形** | hover 左侧栏（展开） | 56×56 方形 logo + 右侧渐显"票次元"文字；移开 → 文字渐隐；logo 始终保持 1:1 不拉伸 |
| **Sidebar z-index 正确** | hover 左/右侧栏展开 → 看是否能盖住 Header / SearchBar dropdown / 顶部 sticky；再开 ConfirmDialog（如取关 / 退群）→ Dialog 仍在最上 | 侧栏盖住 Header 等 z<150 元素；Dialog (z-200) / ImageGallery (z-300) / Toast (z-9999) 仍在侧栏之上 |
| **文字颜色对比度** | 浏览 /messages、/notifications、/tickets、/u/[id] 等页面；DevTools 检查 computed color | 之前看不清的 `text-[#1a1a1f]/30 /40 /60` 现在统一为 `text-foreground-faint`（48% 不透明）/ `text-foreground-soft`（72%）；可读性明显提升 |
| **`grep 0 hit`** | 跑 `grep -rn 'text-\[#1a1a1f\]' apps/web` | 输出 0 行（已完全替换为语义类）|
| **群聊列表搜索** | /messages → 搜"测" | 群聊名字含"测"的也能搜出来（不只私聊）|
| **`/messages/[id]` 群聊场景** | 进群聊会话 → 发文字 + 发图 | 不报错；header 显示群名 + 成员数；点击 header 跳群详情页 |

### 2026-05-11 — 群聊高级权限 + Mobile 排查指南 + 版本号统一

| 改动 | 测试入口 | 期望效果 |
|---|---|---|
| **提升 / 撤销管理员（Web）** | A（owner）进 /messages/groups/[id] → 点 member 行右侧 ⋮ → "设为管理员" | B 的 role badge 变蓝色 admin；再点 ⋮ → "撤销管理员" → 二次确认 → 变回灰色 member |
| **禁言 / 取消禁言（Web）** | owner / admin 点非 owner 成员 ⋮ → "禁言" | 二次确认 → 卡片名旁出现 🔇 橙色 icon；用被禁言账号去群里发消息 → 收到 403 "您已被群主或管理员禁言" |
| **转让群主（Web）** | owner ⋮ 任意非 owner 成员 → "转让群主" | 黄色二次确认（提示"转让后您将变为普通管理员"）→ 确认 → A 降为 admin、目标升为 owner；Conversation.creatorId 也变 |
| **设置群昵称（Web）** | 自己 ⋮ → "设置我的群昵称" / owner-admin ⋮ 他人 → "设置群昵称" | Modal 弹出 textarea（最长 20 字符）→ 保存 → 成员行显示 `群昵称 (主昵称)` 形式；留空保存 = 清除 |
| **群聊高级权限（Mobile）** | Expo Go 进 GroupDetailScreen → 点（或长按）成员行 | 弹 ActionSheet modal（标题 + 多按钮 + 取消）；权限分级跟 Web 一致；群昵称用独立 modal |
| **Mobile 启动排查指南** | 直接读 docs/Mobile-排查指南.md | 8 个 section 齐：工具链版本 / 启动流程 / 7 大常见问题 / 登录细节 / 健康检查 / 调试 / EAS / follow-up |
| **CLAUDE.md Mobile 铁律** | grep "📱 Mobile 启动" CLAUDE.md | 看到压缩 5 条 + 指向 docs/Mobile-排查指南.md |
| **版本号统一** | `node -v`、grep `three` apps/*/package.json、grep `@types/react` apps/web/package.json | Node 22+；three 两端都是 `^0.182.0`；web @types/react 是 `~19.1.10` |

### 2026-05-03 — 群聊补全 + 测试文档化

| 改动 | 测试入口 | 期望效果 |
|---|---|---|
| **群聊在 /messages 列表显示** | A 创建群（"+ 群聊"）→ 加 B C → 返回 /messages | 看到群聊条目（紫粉 👥 + 群名 + "N 人" 标签 + 最近消息），点击进群 |
| **群聊加成员** | 群主进 /messages/groups/[id] → 成员区右上"+ 添加成员" | Modal 弹出 → 搜用户 → 多选 → 已在群的用户灰色不可选 → 添加成功 toast |
| **群聊踢人** | 同上页面，非自己非 owner 的成员右侧 UserMinus 按钮 | 点击 ConfirmDialog → 确认移除 → 成员消失 |
| **群聊改头像** | 编辑铅笔 → 上传新头像 → 保存 | 头像更新，列表 + 群详情都生效 |
| **测试流程文档** | 直接读 docs/手动测试流程.md | 包含触发时机矩阵 / 测试分类 / 已 ship 功能表 |

### 2026-05-03 — Batch 1-4（admin 收藏品 / 私聊发图 / 通知 / 票务 / 搜索 / 群聊）

| 改动 | 测试入口 | 期望效果 |
|---|---|---|
| **Admin 添加收藏品** | 用 13900000099 → /admin/collectibles → "+ 添加收藏品" | Dialog：名/描述/图/分类/数量 + 关联活动可选；提交后列表多一条 |
| **私聊发图（Web）** | A → /messages/[B] → 输入栏左侧图标 → 选图 | 立即上传 + 乐观气泡 → 对方实时收到；点图全屏 lightbox |
| **私聊发图（Mobile）** | Expo Go A → ChatScreen → 左侧 image-outline 图标 | 同上流程；Bubble 渲染 220×220 + 点击 Modal 全屏 |
| **/notifications 通知页** | 任一登录账号 → /notifications | 8 个 filter tab；点条目标已读 + 跳 link；右上"全部标已读" |
| **/tickets 票列表 + 详情** | 登录 → /tickets（需先有票）→ 点开详情 | 列表（封面/状态/价格）→ 详情（banner + QR + 转赠/退款） |
| **票转赠 + 接受** | /tickets/[id]/transfer → 留空对方手机 → 复制链接 → 另号打开接收 | 接收后原账号 /tickets 看不到，新账号能看到 |
| **/search 综合搜索** | 顶栏搜索 "测试" 回车 / 下拉"搜索全部" | /search → user/post/event 三 tab 都有结果 |
| **群聊创建** | /messages → "+ 群聊" → 表单 → 创建 | 跳转到新群聊会话，header 显示群名 + 成员数 |
| **群聊详情** | 群聊会话 header 头像 / 名 → 点击 | 群信息 + 成员列表 + 退出/解散 |
| **关注/粉丝列表** | /u/[id] → 粉丝/关注数字点击 | 列表页 + 头像 + bio + 关注/取关按钮（取关二次确认）|

### Sprint 5（2026-05-02）— UX 长尾收尾

| 改动 | 测试入口 | 期望效果 |
|---|---|---|
| **W-N5 滚动回顶按钮** | 任一长页滚 > 600px | 右下出现 ↑ 紫色圆按钮，点击平滑回顶 |
| **W-S2 发私信按钮** | 进 B/C/D 主页 | 头部"发私信"按钮 → 跳 /messages/[convId] |
| **W-S7 互关 badge** | A 进 B 主页 | "互相关注"粉色徽章；进 C 主页"TA 关注了你" + "回关"按钮 |
| **W-N4 详情面包屑** | 进任一活动 / 帖子 / 用户主页 | 顶部出现面包屑导航 |
| **W-T4 ext 取关确认** | /favorites → 我关注的 → 取关 | 弹 ConfirmDialog（不是浏览器原生）|
| **W-N3 路由骨架屏** | 切 /events ↔ /encore ↔ /signals 首次 | 不闪白屏，看到骨架屏 |
| **W-L3 空状态** | 新账号进 /favorites（无收藏） | 友好图标 + 描述 + CTA 按钮 |
| **W-P2 字体策略** | DevTools → Network → 看 Geist 字体加载 | font-display: swap，无 FOIT |
| **W-A4 表单 a11y** | /account/settings 改密码故意输错 | aria-live 让屏幕阅读器读出错误 |
| **M-L5 Mobile 回顶 FAB** | Mobile feed 滚 > 600px | 右下出 ↑ FAB |
| **M-L3 Mobile 骨架屏** | Mobile 进 Conversations / Notifications / Events | 初次加载是骨架屏，不是 spinner |
| **M-S3 取关 Alert 确认** | Mobile A 主页找 B → 取关 | Alert.alert 二次确认（destructive 红按钮）|

### Sprint 2-3（之前 sprint）

| 改动 | 测试入口 | 期望效果 |
|---|---|---|
| **W-S4 lightbox** | 帖子详情点图 | 全屏 lightbox + 键盘 ←/→/ESC + 点空白关闭 |
| **W-S3 评论分页** | 帖子（>10 条评论） | 底部"加载更多"按钮 + 顶层评论懒加载回复 |
| **W-S5 私信分页 / 乐观发送** | A↔B 80 条历史私信会话 | 顶部"加载更早消息"；发消息：sending → 实心；离线发送红边 + ↻ |
| **W-T3 离线 banner** | DevTools → Offline | 顶部红 banner；恢复后绿 banner 1.5s |
| **W-L1 图片懒加载** | DevTools Network 面板 | 封面图走 /_next/image?url=...&w=...&q=75 |
| **W-P4 AVIF/WebP** | Network 看图 Response Header | Content-Type: image/avif（Chrome）|
| **W-L4 错误页** | 访问 /encore/不存在的id | error.tsx 显示 message + digest + 重试按钮 |

---

## 综合 e2e 测试流程（Web 完整跑一遍约 15 分钟）

按顺序操作，覆盖核心 user journey：

1. **登录** 13900000001 / Test123456!（W-F2/F5/A4）
2. **主页** /events → 看 active 高亮 + 卡片显示（W-N1/L1/P1）
3. **宇宙信号** /signals → 切类目 → 进一个活动详情（W-N4 面包屑）
4. **购票** 选数量 → checkout → 顶部返回按钮可见
5. **/encore 安可区** 进帖子 → 点图 lightbox（W-S4）→ 评论分页（W-S3）
6. **/u/[B] 用户主页** → 互关 badge（W-S7）→ 发私信按钮（W-S2）
7. **私信会话** 加载更早消息 → 发文字 → 发图片 → 失败重试
8. **/messages** 列表显示**群聊**（点 + 群聊创建）
9. **/notifications** 通知页 → 标已读
10. **/search** 顶栏搜索 → 综合结果页
11. **/tickets** 票列表 → 详情 → QR → 转赠 → 接收账号验证
12. **/admin/collectibles**（用 13900000099）添加收藏品
13. **退出登录** → ConfirmDialog（W-T4）

---

## 新功能 ship 后的检查清单

每次 ship 必须：

1. ✅ **本文件加入"已 ship 的功能测试入口"**：在最近一组 batch 表格加新行（改动 ID / 测试入口 / 期望效果）
2. ✅ **如新增 seed 数据**：同步改 `apps/web/scripts/seed-test-data.js` + 更新 [测试数据使用说明.md](测试数据使用说明.md)
3. ✅ **如改 admin 流程**：同步改 [admin-功能测试清单.md](admin-功能测试清单.md)
4. ✅ **如改跨端共享逻辑（同时影响 mobile + web）**：同步改 [Web-Mobile功能对齐审计.md](Web-Mobile功能对齐审计.md)
5. ✅ **如新增冒烟级 P0 流程**：同步改 [冒烟测试清单.md](冒烟测试清单.md)

漏更新算 ship 不完整。下次 session（包括其他 Claude 实例）会通过本文找到该测什么，没列出的功能视为"未交付"。
