# 管理员权限分配操作手册（含详细运行位置）

## 👤 你的管理员账号

```
手机号：17701790343
角色：admin
密码：（你自己设置的密码）
```

⚠️ **重要**：第一次使用管理员功能前，需要**重新登录**以获取包含admin权限的新token！

---

## 📖 运行环境说明

本手册中的代码需要在不同地方运行，请注意每段代码前的标注：

| 标注 | 说明 | 示例 |
|------|------|------|
| 🌐 **浏览器** | 在浏览器打开网址 | Chrome, Firefox, Edge |
| 🖥️ **浏览器控制台** | 按F12打开开发者工具Console标签 | JavaScript代码 |
| ⚡ **Git Bash** | Windows的Git Bash终端 | bash/curl命令 |
| 💻 **PowerShell** | Windows PowerShell终端 | 部分bash命令 |
| 📱 **Postman** | Postman API测试工具 | HTTP请求 |

---

## 🚀 基础操作

### 步骤1：重新登录获取管理员token

你的账号已经是admin，但需要重新登录获取新token：

#### 方式1：通过网页登录（推荐新手）

🌐 **在哪里运行**：浏览器（Chrome/Firefox/Edge）

1. 在浏览器地址栏输入：`http://localhost:3000/auth/login`
2. 输入手机号：`17701790343`
3. 输入你的密码
4. 点击登录按钮
5. 登录成功后，浏览器会自动保存新token到localStorage

#### 方式2：通过API登录

⚡ **在哪里运行**：Git Bash（Windows）或终端（Mac/Linux）

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "17701790343",
    "password": "Liniuniu@7626"
  }'
```

💡 **如何打开Git Bash**：
- 在桌面或任意文件夹右键 → 选择"Git Bash Here"
- 或按Windows键，搜索"Git Bash"

登录成功后，返回的token中就包含 `role: 'admin'` 了。

---

### 步骤2：验证管理员权限

🖥️ **在哪里运行**：浏览器开发者控制台（按F12 → Console标签）

```javascript
// 查看当前token
const token = localStorage.getItem('token');
console.log('Token:', token);

// 解码查看内容（token是JWT格式）
const payload = JSON.parse(atob(token.split('.')[1]));
console.log('用户信息:', payload);
// 应该看到 role: 'admin'
```

💡 **如何打开浏览器控制台**：
1. 在网页上按 `F12` 键
2. 或右键 → "检查" → 点击"Console"标签
3. 在Console标签的输入框粘贴上面的代码
4. 按Enter执行

---

## 🔍 查看用户信息

### 方法1：通过数据库查看（最直接）

⚡ **在哪里运行**：Git Bash

#### 查看所有用户

```bash
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "SELECT id, phone, email, nickname, role, \"createdAt\" FROM users ORDER BY \"createdAt\" DESC LIMIT 20;"
```

#### 搜索特定用户（按手机号）

```bash
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "SELECT id, phone, email, nickname, role FROM users WHERE phone = '13800138000';"
```

#### 搜索特定用户（按昵称）

```bash
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "SELECT id, phone, email, nickname, role FROM users WHERE nickname LIKE '%张三%';"
```

#### 按角色筛选

```bash
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "SELECT id, phone, nickname, role FROM users WHERE role = 'staff';"
```

#### 统计各角色数量

```bash
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "SELECT role, COUNT(*) as count FROM users GROUP BY role;"
```

**输出示例**：
```
                  id                  |    phone    |     nickname     | role
--------------------------------------+-------------+------------------+-------
 d5b8dd25-f414-4352-9229-14337ecad259 | 17701790343 | liniuniu-1号     | admin
 a1b2c3d4-e5f6-7890-abcd-ef1234567890 | 13800138000 | 工作人员小李     | user
 f1e2d3c4-b5a6-9876-dcba-0987654321ef | 13900139000 | 工作人员小王     | user
```

从输出中**复制需要修改的用户ID**（第一列的长字符串）。

---

## ⚡ 分配权限

### 方法1：使用curl命令

⚡ **在哪里运行**：Git Bash

#### 第1步：获取你的admin token

🖥️ **先在浏览器控制台运行**（按F12 → Console）：

```javascript
localStorage.getItem('token')
```

复制输出的token（一长串字符）。

#### 第2步：修改用户角色

⚡ **回到Git Bash运行**：

```bash
# 替换下面的{用户ID}和{你的admin_token}
curl -X PUT http://localhost:3000/api/admin/users/{用户ID}/role \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {你的admin_token}" \
  -d '{"role": "staff"}'
```

**完整示例**（假设要将用户提升为工作人员）：

```bash
curl -X PUT http://localhost:3000/api/admin/users/a1b2c3d4-e5f6-7890-abcd-ef1234567890/role \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -d '{"role": "staff"}'
```

**成功响应**：
```json
{
  "ok": true,
  "message": "用户角色已更新：user → staff",
  "data": {
    "userId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "phone": "13800138000",
    "nickname": "工作人员小李",
    "oldRole": "user",
    "newRole": "staff"
  }
}
```

---

### 方法2：使用Postman（推荐）

📱 **在哪里运行**：Postman应用

💡 **Postman安装**：
- 下载地址：https://www.postman.com/downloads/
- 或使用网页版：https://web.postman.co/

**操作步骤**：

1. **打开Postman**

2. **新建请求**（点击"New" → "HTTP Request"）

3. **设置请求方法和URL**：
   - Method选择：`PUT`
   - URL填写：`http://localhost:3000/api/admin/users/{用户ID}/role`
   - 替换`{用户ID}`为实际的用户ID

4. **设置Headers**（点击"Headers"标签）：
   | Key | Value |
   |-----|-------|
   | `Content-Type` | `application/json` |
   | `Authorization` | `Bearer {你的token}` |

5. **设置Body**（点击"Body"标签）：
   - 选择：`raw`
   - 格式选择：`JSON`
   - 输入：
     ```json
     {
       "role": "staff"
     }
     ```

6. **点击Send按钮**

---

### 方法3：直接修改数据库（最快但需谨慎）

⚡ **在哪里运行**：Git Bash

#### 提升为工作人员

```bash
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "UPDATE users SET role = 'staff' WHERE phone = '13800138000' RETURNING id, phone, nickname, role;"
```

#### 提升为管理员（谨慎！）

```bash
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "UPDATE users SET role = 'admin' WHERE phone = '13900139000' RETURNING id, phone, nickname, role;"
```

#### 降级为普通用户

```bash
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "UPDATE users SET role = 'user' WHERE phone = '13800138000' RETURNING id, phone, nickname, role;"
```

⚠️ **重要**：直接修改数据库后，用户需要**重新登录**才能获得新权限！

---

## 📝 常见场景（完整流程）

### 场景1：新员工入职，需要验票权限

#### 步骤1：让员工注册

🌐 **员工操作**：在浏览器打开 http://localhost:3000/auth/register 注册账号

#### 步骤2：查找员工账号

⚡ **管理员在Git Bash运行**：

```bash
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "SELECT id, phone, nickname FROM users WHERE phone = '13800138001';"
```

复制输出的用户ID。

#### 步骤3：提升权限

⚡ **继续在Git Bash运行**：

```bash
# 先获取token（从浏览器控制台复制）
# 替换{复制的ID}和{你的token}
curl -X PUT http://localhost:3000/api/admin/users/{复制的ID}/role \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {你的token}" \
  -d '{"role": "staff"}'
```

#### 步骤4：通知员工重新登录

📧 **告诉员工**："你的账号已升级为工作人员，请退出后重新登录。"

---

### 场景2：员工离职，撤销权限

⚡ **在Git Bash运行**：

```bash
# 降级为普通用户
curl -X PUT http://localhost:3000/api/admin/users/{员工ID}/role \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {你的token}" \
  -d '{"role": "user"}'
```

---

### 场景3：添加第二个管理员（备份）

⚡ **在Git Bash运行**：

```bash
# ⚠️ 谨慎操作！确认对方可信
curl -X PUT http://localhost:3000/api/admin/users/{用户ID}/role \
  -H "Content-Type: application/json" \
  -H "Authorization": Bearer {你的token}" \
  -d '{"role": "admin"}'
```

---

### 场景4：批量查看工作人员

⚡ **在Git Bash运行**：

```bash
# 查看所有工作人员
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "SELECT phone, nickname, role, \"createdAt\" FROM users WHERE role IN ('staff', 'admin') ORDER BY role, \"createdAt\";"
```

---

## ✅ 最佳实践

### 1. 保存你的管理员Token

🖥️ **在浏览器控制台运行**（F12 → Console）：

```javascript
localStorage.getItem('token')
```

复制输出的token，保存到安全的地方（例如：密码管理器或文本文件）。

---

### 4. 定期审计

每月检查一次权限。

⚡ **在Git Bash运行**：

```bash
# 查看所有管理员和工作人员
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "
    SELECT
      phone,
      nickname,
      role,
      \"createdAt\" as registered_date,
      \"updatedAt\" as last_updated
    FROM users
    WHERE role != 'user'
    ORDER BY role, \"createdAt\";
  "
```

---

## 🔧 故障排查

### 问题1：提示"仅管理员可以操作"

**原因**：你的token不包含admin角色

**解决**：
1. 🌐 在浏览器退出登录
2. 🌐 重新登录 http://localhost:3000/auth/login
3. 🖥️ 在控制台验证：`JSON.parse(atob(localStorage.getItem('token').split('.')[1]))`

---

### 问题3：找不到用户ID

#### 方法1：通过手机号查找

⚡ **在Git Bash运行**：

```bash
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "SELECT id FROM users WHERE phone = '13800138000';"
```

#### 方法2：通过昵称模糊搜索

⚡ **在Git Bash运行**：

```bash
PGPASSWORD="Liniuniu@7626" "/c/Program Files/PostgreSQL/18/bin/psql.exe" \
  -U postgres -d piaociyuan \
  -c "SELECT id, phone, nickname FROM users WHERE nickname LIKE '%李%';"
```

---

### 问题4：curl命令报错

**检查清单**：
- [ ] token是否正确（从浏览器控制台复制完整）
- [ ] URL是否正确（用户ID是否复制完整，没有多余空格）
- [ ] Content-Type是否设置
- [ ] JSON格式是否正确（注意引号和逗号）

**调试方法**：

⚡ **在Git Bash运行**（添加 -v 参数查看详细信息）：

```bash
curl -v -X PUT http://localhost:3000/api/admin/users/{ID}/role \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {token}" \
  -d '{"role": "staff"}'
```

---

## 📊 快速参考卡

### 常用命令速查表

| 操作 | 运行位置 | 命令 |
|------|---------|------|
| 获取token | 🖥️ 浏览器控制台 | `localStorage.getItem('token')` |
| 查看所有用户 | ⚡ Git Bash | `PGPASSWORD="..." psql ... -c "SELECT * FROM users;"` |
| 提升为staff | ⚡ Git Bash | `curl -X PUT .../role ... -d '{"role":"staff"}'` |
| 提升为admin | ⚡ Git Bash | `curl -X PUT .../role ... -d '{"role":"admin"}'` |
| 降级为user | ⚡ Git Bash | `curl -X PUT .../role ... -d '{"role":"user"}'` |

### 三个角色

| 角色 | 权限 | 谁可以分配 |
|-----|------|----------|
| user | 购票、查看订单 | 自动（注册时） |
| staff | user + 验票 | admin |
| admin | staff + 改权限 | admin（需谨慎）|

---

## 🎉 总结

作为管理员，你的主要工作流程：

```
1. 🖥️ 浏览器控制台 → 获取admin token → 复制保存
2. ⚡ Git Bash → 查看用户列表 → 复制用户ID
3. ⚡ Git Bash → 使用curl修改权限 → 确认成功
4. 📧 通知用户 → 让他们重新登录
```

### 必备工具

- 🌐 **浏览器**（Chrome/Firefox/Edge）
- ⚡ **Git Bash**（Windows）或终端（Mac/Linux）
- 📱 **Postman**（可选，但推荐）

### 常用组合键

- `F12` - 打开浏览器开发者工具
- `Ctrl+C` - 复制选中内容
- `Ctrl+V` - 粘贴
- `Enter` - 执行命令

现在你已经掌握了所有权限管理的方法！🎊

---

## 💡 温馨提示

### 第一次使用？按这个顺序操作：

1. ✅ **重新登录**（🌐 浏览器）
2. ✅ **验证权限**（🖥️ 控制台）
3. ✅ **保存token**（🖥️ 控制台 → 文本文件）
4. ✅ **查看用户**（⚡ Git Bash）
5. ✅ **尝试修改**（⚡ Git Bash 或 📱 Postman）

### 遇到问题？

1. **看清运行位置**：每段代码前都有标注（🌐/🖥️/⚡/📱）
2. **检查拼写**：命令要完整复制，不要手打
3. **替换占位符**：记得替换`{用户ID}`和`{token}`
4. **重新登录试试**：大部分问题重新登录能解决

---

## 🚀 下一步

建议开发Web管理界面，让操作更简单：
- 不需要记命令
- 不需要切换窗口
- 点击按钮就能改权限
- 更不容易出错

预计开发时间：1-2天
优先级：⭐⭐⭐⭐⭐
