# 文件夹结构对比 - 迁移前后

## 📁 迁移前（现在）

### 当前文件夹位置
```
C:\piaoyuzhou\                    ← 你的工作目录
├── app/                          ← Next.js 页面
├── components/                   ← React 组件
├── lib/                          ← 工具函数
├── hooks/                        ← 自定义 Hooks
├── prisma/                       ← 数据库
├── public/                       ← 静态文件
├── docs/                         ← 文档
├── scripts/                      ← 脚本
├── server.js                     ← WebSocket 服务器
├── package.json                  ← 依赖配置
├── next.config.ts                ← Next.js 配置
├── .env                          ← 环境变量
└── ...
```

### 如何运行
```bash
cd C:\piaoyuzhou
npm run dev          # 启动 Web 服务器
```

---

## 📁 迁移后（Monorepo）

### 新的文件夹位置
```
C:\piaoyuzhou\                              ← 还是这个文件夹（根目录）
├── apps/
│   ├── web/                               ← 你的 Web 项目（原来的全部内容）
│   │   ├── app/                          ← Next.js 页面
│   │   ├── components/                   ← React 组件
│   │   ├── lib/                          ← 工具函数（Web 专用）
│   │   ├── hooks/                        ← Hooks（Web 专用）
│   │   ├── prisma/                       ← 数据库
│   │   ├── public/                       ← 静态文件
│   │   ├── package.json                  ← Web 的依赖
│   │   ├── next.config.ts                ← Next.js 配置
│   │   ├── .env                          ← 环境变量
│   │   └── ...
│   │
│   └── mobile/                            ← 你的 App 项目（未来创建）
│       ├── src/
│       │   ├── screens/                  ← App 页面
│       │   ├── components/               ← App 组件
│       │   └── navigation/               ← App 导航
│       ├── android/                      ← Android 配置
│       ├── ios/                          ← iOS 配置
│       ├── package.json                  ← App 的依赖
│       └── ...
│
├── packages/
│   └── shared/                            ← 共享代码（Web + App 都用）
│       ├── src/
│       │   ├── api/                      ← API 调用（共享）
│       │   ├── hooks/                    ← Hooks（共享）
│       │   ├── utils/                    ← 工具函数（共享）
│       │   ├── types/                    ← TypeScript 类型（共享）
│       │   └── constants/                ← 常量（共享）
│       └── package.json
│
├── docs/                                  ← 文档（保持不变）
├── server.js                              ← WebSocket 服务器（保持不变）
├── package.json                           ← 根配置（管理整个 Monorepo）
└── turbo.json                             ← Turborepo 配置（新增）
```

---

## 🎯 关键对比

### Web 项目位置

| 项目 | 迁移前 | 迁移后 |
|------|--------|--------|
| **根目录** | `C:\piaoyuzhou\` | `C:\piaoyuzhou\` ✅ 不变 |
| **Web 代码** | `C:\piaoyuzhou\` | `C:\piaoyuzhou\apps\web\` ⬇️ 下移一层 |
| **工作目录** | `cd C:\piaoyuzhou` | `cd C:\piaoyuzhou\apps\web` |

### App 项目位置（未来）

| 项目 | 位置 |
|------|------|
| **App 代码** | `C:\piaoyuzhou\apps\mobile\` |
| **工作目录** | `cd C:\piaoyuzhou\apps\mobile` |

### 共享代码位置（新增）

| 项目 | 位置 |
|------|------|
| **共享代码** | `C:\piaoyuzhou\packages\shared\` |
| **工作目录** | `cd C:\piaoyuzhou\packages\shared` |

---

## 💻 如何运行项目

### 迁移前（现在）
```bash
# 启动 Web 服务器
cd C:\piaoyuzhou
npm run dev
```

### 迁移后（Monorepo）

#### 方式 1: 在根目录运行（推荐）
```bash
cd C:\piaoyuzhou

# 同时运行 Web 和 App（如果都存在）
npm run dev

# 只运行 Web
npm run dev:web

# 只运行 App
npm run dev:mobile
```

#### 方式 2: 在子项目运行
```bash
# 运行 Web
cd C:\piaoyuzhou\apps\web
npm run dev

# 运行 App
cd C:\piaoyuzhou\apps\mobile
npm start
```

---

## 📂 VS Code 工作区配置

### 迁移前（现在）
```
打开文件夹: C:\piaoyuzhou
```

### 迁移后

#### 选项 1: 打开整个 Monorepo（推荐⭐⭐⭐⭐⭐）
```
打开文件夹: C:\piaoyuzhou

优势：
✅ 可以看到 Web + App + Shared 的所有代码
✅ 可以搜索整个项目
✅ 方便同时修改多个项目
```

#### 选项 2: 打开单个项目
```
打开文件夹: C:\piaoyuzhou\apps\web      （只看 Web）
打开文件夹: C:\piaoyuzhou\apps\mobile   （只看 App）
```

#### 选项 3: 使用 VS Code Workspace（最佳⭐⭐⭐⭐⭐）
创建 `piaoyuzhou.code-workspace`:
```json
{
  "folders": [
    {
      "name": "🌐 Web",
      "path": "apps/web"
    },
    {
      "name": "📱 Mobile",
      "path": "apps/mobile"
    },
    {
      "name": "🔧 Shared",
      "path": "packages/shared"
    }
  ],
  "settings": {
    "typescript.tsdk": "apps/web/node_modules/typescript/lib"
  }
}
```

**使用**:
```
File → Open Workspace from File → piaoyuzhou.code-workspace
```

**效果**:
```
左侧边栏显示：
📁 🌐 Web
📁 📱 Mobile
📁 🔧 Shared
```

---

## 🗂️ Git 仓库

### 迁移前（现在）
```
C:\piaoyuzhou\.git                ← 一个 Git 仓库
```

### 迁移后
```
C:\piaoyuzhou\.git                ← 还是一个 Git 仓库（不变）

包含：
- apps/web/
- apps/mobile/
- packages/shared/

一次提交，全部更新
```

---

## 📦 依赖管理

### 迁移前（现在）
```bash
cd C:\piaoyuzhou
npm install                       # 安装所有依赖
```

### 迁移后

#### 方式 1: 在根目录安装所有依赖（推荐）
```bash
cd C:\piaoyuzhou
npm install                       # 自动安装 web + mobile + shared 的所有依赖
```

#### 方式 2: 单独安装
```bash
# 只安装 Web 的依赖
cd C:\piaoyuzhou\apps\web
npm install

# 只安装 App 的依赖
cd C:\piaoyuzhou\apps\mobile
npm install

# 只安装 Shared 的依赖
cd C:\piaoyuzhou\packages\shared
npm install
```

---

## 🔍 实际路径示例

### 迁移前：打开一个文件
```
文件: C:\piaoyuzhou\app\events\page.tsx

VS Code 显示:
📁 piaoyuzhou
  📁 app
    📁 events
      📄 page.tsx              ← 你打开的文件
```

### 迁移后：打开同一个文件
```
文件: C:\piaoyuzhou\apps\web\app\events\page.tsx

VS Code 显示:
📁 piaoyuzhou
  📁 apps
    📁 web
      📁 app
        📁 events
          📄 page.tsx          ← 你打开的文件
```

**变化**: 路径中多了 `apps/web/`

---

## 🚀 实际工作流程

### 场景 1: 开发 Web 功能

**迁移前**:
```bash
cd C:\piaoyuzhou
code .                           # 打开 VS Code
npm run dev                      # 启动服务器
# 编辑 app/events/page.tsx
```

**迁移后**:
```bash
cd C:\piaoyuzhou
code .                           # 打开整个 Monorepo
npm run dev:web                  # 启动 Web 服务器
# 编辑 apps/web/app/events/page.tsx
```

---

### 场景 2: 开发 App 功能（未来）

**迁移后**:
```bash
cd C:\piaoyuzhou
code .                           # 打开整个 Monorepo
npm run dev:mobile               # 启动 App
# 编辑 apps/mobile/src/screens/EventsScreen.tsx
```

---

### 场景 3: 修改共享代码（影响 Web + App）

**迁移后**:
```bash
cd C:\piaoyuzhou
code .                           # 打开整个 Monorepo

# 修改共享 API
# 编辑 packages/shared/src/api/events.ts

# Web 和 App 自动同步更新 ✅
```

---

## 📌 重要提示

### ✅ 不会变的
1. **根目录位置**: 还是 `C:\piaoyuzhou`
2. **Git 仓库**: 还是同一个 Git 仓库
3. **所有文件**: 所有文件都在，只是移动了位置
4. **功能**: 所有功能完全一样

### ⚠️ 会变的
1. **Web 代码位置**: `C:\piaoyuzhou\` → `C:\piaoyuzhou\apps\web\`
2. **工作目录**: 需要 `cd apps/web` 或在根目录用脚本
3. **导入路径**: 某些导入会从 shared 包引用

### 🎯 总结对比表

| 操作 | 迁移前 | 迁移后 |
|------|--------|--------|
| **根目录** | `C:\piaoyuzhou` | `C:\piaoyuzhou` ✅ |
| **打开项目** | `code C:\piaoyuzhou` | `code C:\piaoyuzhou` ✅ |
| **Web 代码** | `C:\piaoyuzhou\app\` | `C:\piaoyuzhou\apps\web\app\` ⬇️ |
| **App 代码** | ❌ 不存在 | `C:\piaoyuzhou\apps\mobile\` ✅ |
| **共享代码** | ❌ 不存在 | `C:\piaoyuzhou\packages\shared\` ✅ |
| **启动 Web** | `npm run dev` | `npm run dev:web` 📝 |
| **启动 App** | ❌ 不存在 | `npm run dev:mobile` ✅ |
| **Git 提交** | `git commit` | `git commit` ✅ |

---

## 🎬 实际演示

### 你现在打开项目的方式
```bash
# 1. 打开文件夹
code C:\piaoyuzhou

# 2. VS Code 显示
📁 PIAOYUZHOU
  📁 app
  📁 components
  📁 lib
  📁 hooks
  ...
```

### 迁移后打开项目的方式
```bash
# 1. 打开文件夹（完全一样）
code C:\piaoyuzhou

# 2. VS Code 显示
📁 PIAOYUZHOU
  📁 apps
    📁 web            ← 你的 Web 项目
    📁 mobile         ← 你的 App 项目
  📁 packages
    📁 shared         ← 共享代码
  📁 docs
  📄 package.json
  ...
```

---

## 🤔 常见问题

### Q1: 我的 Web 项目还在 C:\piaoyuzhou 吗？
**答**:
- 根目录还在 `C:\piaoyuzhou` ✅
- Web 代码移到了 `C:\piaoyuzhou\apps\web\` ⬇️

### Q2: 我需要换电脑路径吗？
**答**: 不需要！
- 还是 `C:\piaoyuzhou`
- 完全不影响

### Q3: App 在哪里？
**答**: `C:\piaoyuzhou\apps\mobile\`
- 和 Web 在同一个根目录下
- 并列关系

### Q4: 共享代码在哪里？
**答**: `C:\piaoyuzhou\packages\shared\`
- Web 和 App 都引用这里的代码

### Q5: 我打开 VS Code 时打开哪个文件夹？
**答**:
- **最佳**: 打开 `C:\piaoyuzhou`（看到所有项目）
- **也可以**: 打开 `C:\piaoyuzhou\apps\web`（只看 Web）

### Q6: Git 仓库在哪？
**答**: 还是 `C:\piaoyuzhou\.git`
- 一个仓库，管理所有代码
- 提交一次，Web + App + Shared 全部更新

---

## 总结

### 简单来说：

**迁移前**:
```
C:\piaoyuzhou\          ← 你的 Web 项目
```

**迁移后**:
```
C:\piaoyuzhou\          ← 还是这个文件夹
├── apps/web/          ← 你的 Web 项目（原来的全部内容）
├── apps/mobile/       ← 你的 App 项目（未来创建）
└── packages/shared/   ← 共享代码（新增）
```

**你的工作方式几乎不变**:
- ✅ 还是同一个文件夹 `C:\piaoyuzhou`
- ✅ 还是用 VS Code 打开
- ✅ 还是用 Git 提交
- ✅ 只是 Web 代码下移了一层到 `apps/web/`

**额外的好处**:
- ✅ 未来的 App 和 Web 在一起
- ✅ 共享代码自动复用
- ✅ 一次修改，两端生效

清楚了吗？ 😊
