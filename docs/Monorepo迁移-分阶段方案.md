# Monorepo 迁移 - 分阶段零风险方案

## 🎯 核心原则

### ✅ 零风险
- 每个阶段都可以独立运行
- 随时可以停止，不影响现有项目
- 出问题随时可以回退

### ✅ 可中断恢复
- 每个阶段有明确的检查点
- 中断后可以从检查点继续
- 详细的状态追踪文档

### ✅ 渐进式迁移
- 不是一次性迁移
- 可以边开发边迁移
- 随时可以暂停

---

## 📋 迁移阶段划分

### 阶段 0: 备份（5分钟）⚠️ **最重要**

**目标**: 确保万无一失

**操作**:
```bash
# 1. 创建完整备份
cd C:\
cp -r piaoyuzhou piaoyuzhou-backup-20251103

# 2. Git 提交现有代码
cd piaoyuzhou
git add .
git commit -m "迁移前备份"
git tag before-monorepo  # 创建标签，方便回退
```

**检查点**:
- [ ] 备份文件夹已创建
- [ ] Git 已提交
- [ ] Git 标签已创建

**如果中断**: 什么都不会丢失

---

### 阶段 1: 创建基础结构（10分钟）✅ **低风险**

**目标**: 创建 Monorepo 文件夹结构，**不动现有代码**

**操作**:
```bash
cd C:\piaoyuzhou

# 创建新文件夹结构
mkdir -p apps
mkdir -p packages/shared/src

# 创建配置文件（我会生成这些）
# package.json（根目录）
# turbo.json
# packages/shared/package.json
# packages/shared/tsconfig.json
```

**生成的文件**:

`package.json`（根目录）:
```json
{
  "name": "piaoyuzhou-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "cd apps/web && npm run dev",
    "install-all": "npm install"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
```

**检查点**:
- [ ] apps/ 文件夹已创建
- [ ] packages/shared/ 文件夹已创建
- [ ] 根 package.json 已创建
- [ ] 原有代码未动

**测试**: 原项目仍可正常运行
```bash
npm run dev  # 应该还能跑
```

**如果中断**:
- 删除新创建的文件夹即可
- 原项目完全不受影响

---

### 阶段 2: 移动项目到 apps/web（15分钟）✅ **中等风险**

**目标**: 把现有项目移到 `apps/web/`

**操作**:
```bash
# 方式 1: 直接移动（推荐）
cd C:\piaoyuzhou
mkdir apps/web

# 移动所有文件（除了 apps 和 packages 文件夹）
# 我会给你详细的命令列表
```

**详细移动清单**:
```bash
# 移动这些文件夹到 apps/web/
mv app apps/web/
mv components apps/web/
mv lib apps/web/
mv hooks apps/web/
mv prisma apps/web/
mv public apps/web/
mv scripts apps/web/

# 移动配置文件
mv package.json apps/web/
mv package-lock.json apps/web/
mv tsconfig.json apps/web/
mv next.config.ts apps/web/
mv tailwind.config.ts apps/web/
mv .env apps/web/
mv .gitignore apps/web/

# 保留在根目录
# - apps/
# - packages/
# - docs/
# - server.js
# - README.md
```

**检查点**:
- [ ] apps/web/ 包含所有原有代码
- [ ] apps/web/package.json 存在
- [ ] apps/web/app/ 存在

**测试**:
```bash
cd apps/web
npm install
npm run dev  # 应该能正常运行
```

**如果中断**:
```bash
# 回退方案
cd C:\piaoyuzhou
mv apps/web/* .
rmdir apps/web
```

---

### 阶段 3: 创建共享包（20分钟）✅ **低风险**

**目标**: 创建 `packages/shared`，但**不修改 web 代码**

**操作**: 我会创建这些文件

`packages/shared/package.json`:
```json
{
  "name": "@piaoyuzhou/shared",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  }
}
```

`packages/shared/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "declaration": true,
    "outDir": "./dist",
    "strict": true
  },
  "include": ["src"]
}
```

`packages/shared/src/index.ts`:
```typescript
// 空文件，占位
export {};
```

**检查点**:
- [ ] packages/shared/package.json 已创建
- [ ] packages/shared/tsconfig.json 已创建
- [ ] packages/shared/src/index.ts 已创建

**测试**:
```bash
cd packages/shared
npm install
npm run build  # 应该成功
```

**如果中断**: 删除 packages/shared 即可，不影响 web

---

### 阶段 4: 提取第一个共享模块（30分钟）⚡ **关键阶段**

**目标**: 提取**一个**简单的共享模块作为测试

**选择**: 先提取 `types`（最简单，最安全）

#### 4.1 复制类型定义到 shared

**我会创建**:
```typescript
// packages/shared/src/types/event.ts
export interface Event {
  id: string;
  name: string;
  date: string;
  // ... 复制现有类型
}
```

**检查点 4.1**:
- [ ] packages/shared/src/types/ 已创建
- [ ] 类型定义已复制
- [ ] 原 web 代码未修改

#### 4.2 更新 shared 的导出

```typescript
// packages/shared/src/index.ts
export * from './types/event';
export * from './types/user';
```

**检查点 4.2**:
- [ ] shared 可以正确导出类型
- [ ] npm run build 成功

#### 4.3 更新 web 项目使用 shared

**修改** `apps/web/app/events/page.tsx`:
```typescript
// 之前
// import { Event } from '@/types/event';

// 之后
import { Event } from '@piaoyuzhou/shared';
```

**检查点 4.3**:
- [ ] Web 项目引用 shared
- [ ] npm run dev 成功
- [ ] 页面正常显示

**如果失败**:
```bash
# 回退：恢复原来的 import
import { Event } from '@/types/event';
```

**如果中断**:
- **状态文档** 会记录到哪一步
- **下次继续** 从检查点 4.X 继续

---

### 阶段 5-10: 逐步提取其他模块（每个 30-60 分钟）

**每个阶段提取一个模块**:
- 阶段 5: 提取 utils
- 阶段 6: 提取 constants
- 阶段 7: 提取 api（部分）
- 阶段 8: 提取 hooks（部分）
- 阶段 9: 提取 socket
- 阶段 10: 最终清理

**每个阶段都有**:
- 独立的检查点
- 回退方案
- 测试步骤

---

## 📊 状态追踪文档

### migration-status.md

**我会创建并持续更新这个文件**:

```markdown
# Monorepo 迁移状态

## 当前进度
- [x] 阶段 0: 备份完成 ✅
- [x] 阶段 1: 基础结构创建 ✅
- [x] 阶段 2: 移动到 apps/web ✅
- [x] 阶段 3: 创建 shared 包 ✅
- [ ] 阶段 4: 提取 types
  - [x] 4.1 复制类型定义 ✅
  - [x] 4.2 更新导出 ✅
  - [ ] 4.3 更新 web 引用 ⏸️ **中断于此**
- [ ] 阶段 5-10: 待处理

## 最后更新
时间: 2025-11-03 15:30
操作: 正在执行阶段 4.3
下一步: 修改 apps/web/app/events/page.tsx

## 当前可运行状态
✅ apps/web 可以独立运行
✅ 原有功能全部正常
⚠️ 正在迁移中

## 回退指令
如果需要回退到迁移前:
```bash
git checkout before-monorepo
```

## 继续迁移指令
下次继续从这里开始:
1. 打开 apps/web/app/events/page.tsx
2. 修改 import 语句（见下方）
3. 测试运行
```
```

---

## 🔄 中断恢复机制

### 如果我中断了，你这样做：

#### 方案 1: 重新开始会话（推荐）

**你说**:
```
"继续 Monorepo 迁移，当前状态在 docs/migration-status.md"
```

**新的 AI 实例会**:
1. 读取 migration-status.md
2. 了解当前进度
3. 从检查点继续
4. 完成剩余工作

#### 方案 2: 自己继续（如果很简单）

**查看**:
```bash
cat docs/migration-status.md
```

**按照**:
- 下一步指令
- 详细的操作说明
- 继续执行

#### 方案 3: 暂停迁移

**随时可以停下来**:
- apps/web 仍然可以独立运行
- 已完成的阶段保留
- 未完成的阶段不影响使用

---

## 📝 详细操作日志

### migration-log.md

**每个操作都会记录**:

```markdown
## 2025-11-03 14:00 - 阶段 1 开始
操作: 创建基础文件夹结构
命令:
```bash
mkdir -p apps
mkdir -p packages/shared/src
```
结果: ✅ 成功

## 2025-11-03 14:10 - 阶段 2 开始
操作: 移动项目到 apps/web
命令:
```bash
mv app apps/web/
mv components apps/web/
...
```
结果: ✅ 成功

## 2025-11-03 14:25 - 阶段 4.1 开始
操作: 复制类型定义
文件: packages/shared/src/types/event.ts
结果: ✅ 成功

## 2025-11-03 14:35 - 阶段 4.3 开始
操作: 更新 web 引用
文件: apps/web/app/events/page.tsx
行 3: import { Event } from '@piaoyuzhou/shared';
结果: ⏸️ 中断

## 下一步（待执行）
1. 修改 apps/web/app/events/page.tsx 第 3 行
2. 运行 npm run dev 测试
3. 如果成功，继续下一个文件
```
```

---

## ⚠️ 紧急回退方案

### 如果出现严重问题

**立即回退**:
```bash
cd C:\piaoyuzhou
git reset --hard before-monorepo
```

**恢复备份**:
```bash
cd C:\
rm -rf piaoyuzhou
cp -r piaoyuzhou-backup-20251103 piaoyuzhou
```

**两种方式都能让你回到迁移前的状态**

---

## 🎯 实际迁移建议

### 方案 A: 分多天迁移（最安全⭐⭐⭐⭐⭐）

**第一天**（30分钟）:
- 阶段 0: 备份
- 阶段 1: 创建结构
- 阶段 2: 移动到 apps/web
- **测试运行**，确保一切正常

**第二天**（1小时）:
- 阶段 3: 创建 shared 包
- 阶段 4: 提取 types
- **测试运行**，确保一切正常

**第三天**（1小时）:
- 阶段 5-6: 提取 utils 和 constants
- **测试运行**

**第四天**（1-2小时）:
- 阶段 7-10: 提取剩余模块
- **最终测试**

**优势**:
- 每天只做一点
- 随时可以停止
- 风险极低

---

### 方案 B: 只做关键部分（最实用⭐⭐⭐⭐）

**只迁移关键共享代码**:
- 阶段 0-3: 基础结构
- 阶段 4: types
- 阶段 7: api（核心）
- 阶段 9: socket

**保留在 apps/web**:
- UI 组件（本来就不共享）
- 页面代码
- 部分工具函数

**优势**:
- 工作量小
- 已经能享受 60-70% 的好处
- 以后有需要再继续迁移

---

### 方案 C: 暂时不迁移（最保守⭐⭐⭐）

**现在**:
- 继续用现有结构
- 正常开发 Web 版

**等开发 App 时**:
- 再考虑 Monorepo
- 或者只做部分迁移

**优势**:
- 零风险
- 零工作量

---

## 💡 我的建议

### 最佳方案：方案 B（只做关键部分）

**理由**:
1. ✅ 工作量小（2-3小时）
2. ✅ 风险低（分阶段，可回退）
3. ✅ 收益高（60-70% 代码复用）
4. ✅ 灵活（以后可以继续）

**今天就可以开始**:
- 阶段 0: 备份（5分钟）
- 阶段 1-2: 基础结构（30分钟）
- **停下来测试**
- 明天继续或者暂停都可以

---

## 🤝 我的承诺

### 每个阶段我都会：

1. **创建检查点**
   - 明确记录当前状态
   - 更新 migration-status.md
   - 记录详细日志

2. **提供回退方案**
   - 每个操作都可以撤销
   - 出问题立即回退

3. **确保可运行**
   - 每个阶段完成后都能运行
   - 不会出现"半成品"状态

4. **详细文档**
   - 即使我中断，你也能继续
   - 其他 AI 也能接手

---

## 总结

### ✅ 零风险保证

1. **完整备份** - Git + 文件夹备份
2. **分阶段进行** - 每阶段独立可测试
3. **随时可回退** - 一个命令恢复原状
4. **详细文档** - 中断也能继续

### ✅ 可中断恢复

1. **状态追踪** - migration-status.md
2. **操作日志** - migration-log.md
3. **检查点机制** - 每阶段都有
4. **继续指令** - 明确的下一步

### ✅ 灵活选择

1. **分多天** - 最安全
2. **只做关键部分** - 最实用
3. **暂时不做** - 最保守

---

## 现在怎么做？

**选项 1**: "开始阶段 0-2，今天只做基础"
- 30分钟完成
- 低风险
- 随时可停

**选项 2**: "我想先看看详细的 migration-status.md 模板"
- 我立即创建
- 你看看是否满意

**选项 3**: "暂时不迁移，等开发 App 时再说"
- 完全可以
- 保持现状

你选哪个？🚀
