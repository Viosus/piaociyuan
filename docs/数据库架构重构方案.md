# 数据库架构重构方案

## 问题分析

### 当前问题
1. ❌ Badge（纪念品）和 NFT 概念分离 → **实际上是同一个东西**
2. ❌ Event表的NFT字段设计不合理 → NFT应该是独立实体
3. ❌ Order表有NFT字段 → 铸造信息应该在UserNFT表
4. ❌ UserBadge表冗余 → 应该用UserNFT表
5. ❌ 缺少票和NFT的绑定关系
6. ❌ User表个人信息字段不足
7. ❌ 钱包方案只考虑MetaMask → 需要支持国内钱包

### 核心设计原则
- **NFT是一等公民**：Badge表改名为NFT表
- **统一收藏概念**："我的次元" = "我的NFT"
- **支持两种NFT来源**：
  - 🎫 票赠送（ticket_reward）
  - 🛒 独立售卖（standalone）
  - 🎁 空投（airdrop）

---

## 新数据库设计

### 1. NFT表（原Badge表重构）

```prisma
model NFT {
  id                  String   @id @default(uuid())
  name                String   // NFT名称
  description         String   @db.Text // 描述
  imageUrl            String   // 2D图片URL

  // ============ NFT分类 ============
  sourceType          String   // ticket_reward(票赠送), standalone(独立售卖), airdrop(空投)
  category            String   // badge(徽章), ticket_stub(票根), poster(海报), certificate(证书), art(艺术品)

  // ============ 关联信息 ============
  eventId             Int?     // 关联活动（可选，独立售卖的NFT可能没有）
  tierId              Int?     // 关联票档（可选）

  // ============ 经济模型 ============
  rarity              String   @default("common") // common, rare, epic, legendary
  price               Int?     // 独立售卖价格（sourceType=standalone时必填）
  totalSupply         Int      // 总供应量
  mintedCount         Int      @default(0) // 已铸造数量

  // ============ 3D/AR 功能 ============
  has3DModel          Boolean  @default(false)
  model3DUrl          String?  // 3D模型文件URL (glb/gltf)
  modelFormat         String?  // glb, gltf, usdz
  hasAR               Boolean  @default(false)
  arUrl               String?  // AR快速查看URL
  hasAnimation        Boolean  @default(false)
  animationUrl        String?
  modelConfig         String?  @db.Text // 3D配置JSON

  // ============ 区块链信息 ============
  contractAddress     String?  // NFT合约地址（部署后填入）
  tokenIdStart        Int?     // 该系列起始Token ID
  metadataUriTemplate String?  @db.Text // 元数据URI模板 (如 ipfs://xxx/{tokenId})

  // ============ 状态控制 ============
  isActive            Boolean  @default(true)  // 是否启用
  isMintable          Boolean  @default(true)  // 是否可铸造
  isMarketable        Boolean  @default(false) // 是否可在市场交易

  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  // ============ 关联 ============
  event               Event?   @relation(fields: [eventId], references: [id], onDelete: SetNull)
  userNFTs            UserNFT[] // 用户拥有的该NFT实例
  ticketsWithNFT      Ticket[] // 绑定此NFT的票

  @@map("nfts")
  @@index([eventId])
  @@index([sourceType])
  @@index([category])
  @@index([rarity])
  @@index([isActive])
  @@index([isMintable])
}
```

---

### 2. UserNFT表（替代原UserBadge表）

```prisma
model UserNFT {
  id                  String   @id @default(uuid())
  userId              String   // 拥有者
  nftId               String   // NFT模板ID

  // ============ 获得方式 ============
  sourceType          String   // ticket_purchase(买票获得), direct_purchase(直接购买), airdrop(空投), transfer(链上转移)
  sourceId            String?  // 来源ID（订单ID、票ID、交易哈希等）

  // ============ 区块链唯一标识 ============
  contractAddress     String   // 合约地址
  tokenId             Int      // 具体的Token ID（每个用户实例唯一）
  metadataUri         String?  @db.Text // 该Token的元数据URI

  // ============ 所有权信息 ============
  ownerWalletAddress  String   // 当前链上所有者钱包地址
  isOnChain           Boolean  @default(false) // 是否已上链

  // ============ 铸造信息 ============
  mintStatus          String   @default("pending") // pending(待铸造), minting(铸造中), minted(已铸造), failed(失败)
  mintTransactionHash String?  // 铸造交易哈希
  mintedAt            DateTime? // 铸造完成时间
  mintError           String?  @db.Text // 铸造错误信息

  // ============ 转移信息 ============
  isTransferred       Boolean  @default(false) // 是否已转移给他人
  transferredTo       String?  // 转移到的地址
  transferredAt       DateTime? // 转移时间

  // ============ 额外数据 ============
  metadata            String?  @db.Text // JSON格式，存储座位号、序列号等个性化信息

  obtainedAt          DateTime @default(now()) // 获得时间
  lastSyncedAt        DateTime? // 最后同步链上数据的时间

  // ============ 关联 ============
  user                User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  nft                 NFT      @relation(fields: [nftId], references: [id], onDelete: Restrict)

  @@map("user_nfts")
  @@unique([contractAddress, tokenId]) // 链上唯一性
  @@index([userId])
  @@index([nftId])
  @@index([ownerWalletAddress])
  @@index([mintStatus])
  @@index([obtainedAt])
  @@index([sourceType])
}
```

---

### 3. Ticket表修改（增加NFT绑定）

```prisma
model Ticket {
  // ... 原有字段保持不变 ...

  // ============ NFT绑定（新增）============
  nftId               String?  // 绑定的NFT ID（如3D票根NFT）
  nftMintStatus       String?  // 该票的NFT铸造状态 pending/minted
  nftTokenId          Int?     // 该票对应的NFT Token ID

  // ============ 关联修改 ============
  nft                 NFT?     @relation(fields: [nftId], references: [id], onDelete: SetNull)
  // 删除原来的 userBadges 关联

  @@index([nftId])
  @@index([nftMintStatus])
}
```

---

### 4. Order表修改（删除NFT字段）

```prisma
model Order {
  // 保留原有字段
  id, userId, eventId, tierId, qty, holdId, status
  createdAt, paidAt

  // ❌ 删除以下NFT相关字段：
  // nftTransactionHash
  // nftTokenId
  // nftMintedAt
  // nftStatus

  // ✅ NFT信息移到 UserNFT 表，通过 sourceId 关联

  user       User      @relation(...)
  tickets    Ticket[]
  // 删除 userBadges 关联
}
```

---

### 5. User表扩展（个人信息+钱包）

```prisma
model User {
  // ... 原有字段保持 ...

  // ============ 个人信息扩展（新增）============
  bio                 String?  @db.Text // 个人简介/签名
  coverImage          String?  // 个人主页封面图
  website             String?  // 个人网站
  location            String?  // 所在地

  // ============ 认证系统（新增）============
  isVerified          Boolean  @default(false) // 是否认证
  verifiedType        String?  // celebrity(名人), artist(艺术家), organizer(主办方), official(官方)
  verifiedAt          DateTime? // 认证时间
  verificationBadge   String?  // 认证徽章图标URL

  // ============ 钱包信息（修改）============
  walletAddress       String?  @unique // Web3钱包地址
  walletProvider      String?  // metamask, imtoken, tokenpocket, bitkeep, mathwallet, trustwallet
  walletConnectedAt   DateTime?

  // 🇨🇳 支持国内常用钱包：
  // - imToken（国内最流行）
  // - TokenPocket（多链支持）
  // - BitKeep（币安投资）
  // - MathWallet（麦子钱包）
  // - TrustWallet（币安官方）

  // ============ 统计字段（新增）============
  nftCount            Int      @default(0) // NFT总数
  followerCount       Int      @default(0) // 粉丝数
  followingCount      Int      @default(0) // 关注数

  // ============ 关联修改 ============
  userNFTs            UserNFT[] // 改为 userNFTs（替代 userBadges 和 nftAssets）
  // 删除 userBadges 和 nftAssets 关联
}
```

---

### 6. Event表简化（删除NFT字段）

```prisma
model Event {
  // 保留原有字段
  id, name, city, venue, date, time, cover, artist, desc
  createdAt, updatedAt

  // ❌ 删除以下NFT字段：
  // nftEnabled
  // nftContractAddress
  // nftMetadataUri

  // ✅ NFT关联通过 NFT 表的 eventId 字段实现

  // ============ 关联 ============
  tiers               Tier[]
  nfts                NFT[]    // 该活动相关的所有NFT
  posts               Post[]
  followers           EventFollow[]
  notifications       Notification[]

  @@map("events")
}
```

---

### 7. NFTMintQueue表保持（小调整）

```prisma
model NFTMintQueue {
  id                  String   @id @default(uuid())

  // 改为关联 UserNFT 而不是 Order
  userNftId           String   // UserNFT ID
  userId              String

  walletAddress       String
  status              String   @default("pending")
  retryCount          Int      @default(0)
  errorMessage        String?  @db.Text

  createdAt           DateTime @default(now())
  processedAt         DateTime?

  user                User     @relation(...)

  @@map("nft_mint_queue")
  @@index([userNftId])
  @@index([status])
}
```

---

### 8. NFTMarketListing表（新增 - 支持NFT交易市场）

```prisma
model NFTMarketListing {
  id                  String   @id @default(uuid())
  nftId               String   // NFT模板ID
  sellerId            String   // 卖家ID

  price               Int      // 售价
  quantity            Int      // 数量
  sold                Int      @default(0) // 已售出

  status              String   @default("active") // active, sold_out, cancelled

  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  // 关联
  nft                 NFT      @relation(...)
  seller              User     @relation(...)

  @@map("nft_market_listings")
  @@index([nftId])
  @@index([sellerId])
  @@index([status])
}
```

---

## 数据迁移步骤

### Step 1: 创建新表
```bash
# 1. 备份数据库
pg_dump -U postgres piaociyuan > backup_$(date +%Y%m%d).sql

# 2. 创建新schema文件
# 编辑 prisma/schema.prisma

# 3. 生成迁移
npx prisma migrate dev --name restructure_nft_system --create-only

# 4. 手动编辑迁移SQL，添加数据迁移逻辑
```

### Step 2: 数据迁移脚本

```sql
-- 迁移 Badge → NFT
INSERT INTO nfts (
  id, name, description, imageUrl,
  sourceType, category, eventId, tierId,
  rarity, totalSupply, mintedCount,
  has3DModel, model3DUrl, hasAR, arUrl,
  isActive, createdAt, updatedAt
)
SELECT
  id, name, description, imageUrl,
  'ticket_reward' as sourceType,
  type as category,
  "eventId", "tierId",
  rarity,
  1000 as totalSupply, -- 默认供应量
  (SELECT COUNT(*) FROM user_badges WHERE "badgeId" = badges.id) as mintedCount,
  "has3DModel", "model3DUrl", "hasAR", "arUrl",
  "isActive", "createdAt", "updatedAt"
FROM badges;

-- 迁移 UserBadge → UserNFT
INSERT INTO user_nfts (
  id, "userId", "nftId",
  sourceType, "sourceId",
  "contractAddress", "tokenId", "ownerWalletAddress",
  "isOnChain", "mintStatus",
  metadata, "obtainedAt"
)
SELECT
  ub.id,
  ub."userId",
  ub."badgeId" as "nftId",
  CASE
    WHEN ub."ticketId" IS NOT NULL THEN 'ticket_purchase'
    ELSE 'airdrop'
  END as sourceType,
  COALESCE(ub."orderId", ub."ticketId") as "sourceId",
  '0x0000000000000000000000000000000000000000' as "contractAddress", -- 占位
  ROW_NUMBER() OVER (PARTITION BY ub."badgeId" ORDER BY ub."obtainedAt") as "tokenId",
  COALESCE(u."walletAddress", '0x0000000000000000000000000000000000000000') as "ownerWalletAddress",
  false as "isOnChain",
  'pending' as "mintStatus",
  ub.metadata,
  ub."obtainedAt"
FROM user_badges ub
LEFT JOIN users u ON ub."userId" = u.id;

-- 清理旧表
-- DROP TABLE user_badges;
-- DROP TABLE badges;
-- 注意：先不删除，确认迁移成功后再删
```

### Step 3: 应用迁移
```bash
npx prisma migrate dev
```

---

## API路由调整

### 需要修改的路由

1. **`/api/nft/mint/request`**
   - 创建UserNFT记录（mintStatus='pending'）
   - 创建NFTMintQueue记录

2. **新增 `/api/nfts/marketplace`**
   - 浏览独立售卖的NFT
   - 购买流程

3. **`/api/tickets/use`**
   - 检票后自动创建绑定的NFT（如果票有nftId）

---

## 前端页面调整

### 1. "我的次元" 改名为 "我的NFT"

**路由**: `/account/collection` → `/account/nfts`

**显示内容**:
- 所有UserNFT记录
- 按category分类：徽章、票根、海报、艺术品等
- 按rarity筛选
- 支持3D查看器

### 2. NFT市场页面（新增）

**路由**: `/marketplace` 或 `/nft/market`

**功能**:
- 浏览独立售卖的NFT
- 按价格、稀有度排序
- 购买流程

### 3. 票详情页

**显示**:
- 如果票有绑定NFT（nftId不为空），显示NFT预览
- "查看我的3D票根NFT"按钮

---

## 钱包方案（中国市场）

### 推荐钱包

| 钱包 | 优势 | 支持链 |
|------|------|--------|
| **imToken** | 国内用户最多 | ETH, Polygon, BSC等 |
| **TokenPocket** | 多链支持好 | 几乎所有主流链 |
| **BitKeep** | 币安投资，信任度高 | ETH, Polygon, BSC等 |
| **MathWallet** | 老牌钱包 | 多链 |
| **Trust Wallet** | 币安官方钱包 | 多链 |

### 连接方式

```typescript
// lib/wallet-connect.ts
export const SUPPORTED_WALLETS = {
  metamask: {
    name: 'MetaMask',
    available: typeof window !== 'undefined' && window.ethereum?.isMetaMask,
  },
  imtoken: {
    name: 'imToken',
    available: typeof window !== 'undefined' && window.ethereum?.isImToken,
  },
  tokenpocket: {
    name: 'TokenPocket',
    available: typeof window !== 'undefined' && window.ethereum?.isTokenPocket,
  },
  // WalletConnect 作为通用方案
  walletconnect: {
    name: 'WalletConnect',
    available: true, // 总是可用
  },
};
```

### 使用 WalletConnect 协议
- 支持几乎所有移动端钱包
- 扫码连接
- 国内钱包基本都支持

---

## 优势总结

✅ **概念统一**: "次元" = NFT，不再混淆
✅ **支持多种NFT来源**: 票赠送、独立售卖、空投
✅ **票-NFT绑定**: 3D票根等场景
✅ **链上数据追踪**: tokenId、交易哈希等
✅ **支持国内钱包**: imToken等
✅ **扩展性强**: 可以加NFT交易市场
✅ **数据清晰**: 每个表职责明确

---

## 时间估算

- 数据库迁移脚本编写: 2-3小时
- API路由调整: 4-6小时
- 前端页面重构: 6-8小时
- 测试验证: 2-3小时

**总计**: 1-2天完成

---

## 风险提示

⚠️ **数据迁移风险**:
- 先在测试环境验证
- 确保备份完整
- 分步骤迁移，可回滚

⚠️ **用户体验变化**:
- "我的次元"改名需要引导
- 确保旧数据正常显示

⚠️ **区块链集成**:
- 合约地址部署后需要更新
- Token ID分配策略需要明确

---

**是否立即开始重构？**
