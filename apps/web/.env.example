# 数据库连接（PostgreSQL）
# 🔥 重要：请复制此文件为 .env，并修改为您自己的数据库配置
# 注意：密码中的 @ 符号需要 URL 编码为 %40
# 🔥 高并发优化：添加连接池配置
# - connection_limit: 连接池大小（推荐 10-50，根据服务器配置调整）
# - pool_timeout: 获取连接超时时间（秒）
# - connect_timeout: 数据库连接超时时间（秒）
DATABASE_URL="postgresql://用户名:密码@localhost:5432/数据库名?connection_limit=20&pool_timeout=10&connect_timeout=10"

# 示例（请根据实际情况修改）：
# DATABASE_URL="postgresql://postgres:your_password@localhost:5432/piaociyuan?connection_limit=20&pool_timeout=10&connect_timeout=10"

# 如果暂时还没安装 PostgreSQL，可以先用 SQLite（取消下面这行的注释）
# DATABASE_URL="file:./prisma/dev.db"

# JWT 密钥（请生成自己的随机密钥！）
# 生成方法：在终端运行 node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="请在这里填写您生成的 JWT 密钥（128 个字符的随机十六进制字符串）"

# JWT 过期时间
JWT_ACCESS_EXPIRES="15m"    # Access Token: 15分钟
JWT_REFRESH_EXPIRES="7d"    # Refresh Token: 7天

# 敏感信息加密密钥（用于加密手机号、邮箱等）
# 生成方法：在终端运行 node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="请在这里填写您生成的加密密钥（64 个字符的随机十六进制字符串）"

# ==================== 邮件服务 ====================

# Resend 邮件服务（推荐）
# 注册地址：https://resend.com
RESEND_API_KEY="re_xxxxxxxxxx"
RESEND_FROM_EMAIL="票次元 <noreply@piaociyuan.com>"

# ==================== 短信服务 ====================

# 短信服务提供商（mock | aliyun | tencent）
# mock: 仅打印到控制台，用于开发测试
SMS_PROVIDER="mock"

# 阿里云短信服务（当 SMS_PROVIDER="aliyun" 时需要配置）
# ALIYUN_SMS_ACCESS_KEY_ID=""
# ALIYUN_SMS_ACCESS_KEY_SECRET=""
# ALIYUN_SMS_SIGN_NAME="票次元"
# ALIYUN_SMS_TEMPLATE_CODE=""

# 腾讯云短信服务（当 SMS_PROVIDER="tencent" 时需要配置）
# TENCENT_SMS_SECRET_ID=""
# TENCENT_SMS_SECRET_KEY=""
# TENCENT_SMS_SDK_APP_ID=""
# TENCENT_SMS_SIGN_NAME="票次元"
# TENCENT_SMS_TEMPLATE_ID=""
